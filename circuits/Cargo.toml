[package]
name = "privacy-erc20-circuits"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ark-bn254 = "0.4.0"
ark-ff = "0.4.0"
ark-std = "0.4.0"
ark-serialize = "0.4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror.workspace = true
rand.workspace = true
hex.workspace = true

# Poseidon for BN254 (Circom compatible)
poseidon-bn128 = "0.1.1"
scalarff = "0.4.3" # Pinned for poseidon-bn128 compatibility
num-bigint = "0.4"
num-traits = "0.2"

[dev-dependencies]
rand = "0.8"
