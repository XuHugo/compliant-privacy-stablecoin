[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = true
fs_permissions = [{ access = "read", path = ".." }]

# 依赖
[dependencies]
forge-std = "1.9.0"
openzeppelin-contracts = "5.0.0"
